server:
  port: 8082

spring:
  application:
    name: cafeteria

  datasource:
    url: jdbc:postgresql://localhost:5432/cafeteria_bd
    username: postgres
    password: Samuel1242
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true